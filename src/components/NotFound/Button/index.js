import React from "react";

import styles from './Button.module.scss';

export const Button = (props) => {
    return (
        <button type="button" className={styles.orange}>Voltar para a Página Principal</button>
    )
}

